module purge
module load cuda/11.4
module load openmpi/3.1.4-gcc-7.2.0
module load libevent-2.0.21-gcc-7.2.0-s6crwp2

mkdir run 2>/dev/null
# check that input.in.txt is present in run/
mkdir log dat cnfg 2>/dev/null
rm log/* dat/* cnfg/* 2>/dev/null

cd build/
# edit the relevant lines in compile_settings.txt to have the following content:
# CFLAGS -std=c99 -Werror -Wall -g -Og
# LDFLAGS -g
# DYNAMIC_SIZES 1

make all
